---
title: Contribute to Kittynode
description: How to contribute to Kittynode.
---

import { Steps } from '@astrojs/starlight/components';

Kittynode is open to everyone, and happily welcomes any contributions! ヽ(・∀・)ﾉ

## Prerequisites

<Steps>

1. Install [Rust](https://www.rust-lang.org/):

    ```bash
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
    ```

2. Install [just](https://github.com/casey/just):

    ```bash
    cargo install just
    ```

3. Install [bun](https://bun.sh/):

    ```bash
    curl -fsSL https://bun.sh/install | bash
    ```

</Steps>

## Development

<Steps>

1. Clone the repo and change into the directory:

    ```bash
    git clone git@github.com:kittynode/kittynode.git && cd kittynode
    ```

2. Install dependencies:

    ```bash
    bun install
    ```

3. Build the crates:

    ```bash
    just build
    ```

4. See the full list of commands:

    ```bash
    just -l
    ```

</Steps>
